# 표준 피라미드 TIFF 검증 — 2026-09-08

실제 두 샘플을 `output/20260908_142952_473554/`에 저장했습니다.

| 항목 | SVS | NDPI |
|---|---:|---:|
| 원본 바이트 | 1,121,916,033 | 2,111,622,750 |
| 결과 바이트 | 1,066,346,211 | 2,269,857,541 |
| 출력 레벨 수 | 5 | 5 |
| 원본 압축 유지 레벨 | 3 | 3 |
| 추가 생성 레벨 | 2 | 2 |
| 전체 검증 타일 | 111,162 | 262,006 |
| 변환·검증 시간 | 34.5초 | 86.1초 |
| 512px get_thumbnail() 시간 | 0.065초 | 0.120초 |

SVS: 110562×61468 → 27640×15367 → 6910×3841 → 1728×961 → 432×241.

NDPI: 126720×96768 → 31680×24192 → 7920×6048 → 1980×1512 → 495×378.

- SVS: `anonymous_2c5595381a4b45aba6dd2cab69e26aba.tiff`
- NDPI: `anonymous_cacd0c7a3b4348589c8cf0f74ef522ba.tiff`
- `metadata.csv`: 원본/출력 파일명, MPP 등 기술 정보. 두 행 모두 `output_pages=5`.

최대 해상도와 재사용한 원본 축소 레벨의 압축 데이터를 유지하고 JPEG APP/COM 메타데이터를 제거했습니다. 작은 두 레벨만 JPEG 품질 90으로 생성했습니다. NDPI는 원본의 큰 JPEG 스트림을 TIFF 타일로 재배치하면서 헤더가 반복되므로 원본보다 다소 커집니다.

첫 IFD는 최대 해상도, 이후 IFD는 tiled/reduced-resolution입니다. SubIFD와 원본의 라벨·매크로·별도 썸네일은 포함하지 않습니다. 모든 IFD에 구조 태그 허용 목록을 적용하고 ICC·개인정보 태그를 제외합니다.

전체 압축 타일 무결성·디코딩, 최대 해상도 25개 영역의 원본/출력 픽셀 비교, 원본 전체 SHA-256 보존 검사를 통과했습니다. OpenSlide 4.0.1이 5개 레벨을 인식하고 `get_thumbnail((512,512))`는 두 파일 모두 레벨 3을 읽었습니다. 해당 레벨은 400만 픽셀 미만입니다.

합성 테스트는 원본 두 레벨의 압축 데이터 일치, 축소 레벨 생성, 식별자 표식 제거, get_thumbnail의 축소 레벨 접근, 마스크 전파, 생성 중 취소·정리를 검증합니다. 결과 상태는 `metadata_clean_pixel_review_required`이며 영상 속 식별자는 사용자 검토 대상입니다.

기계 판독 기록: `artifacts/pyramid_tiff_validation.json`.
형식 근거: [OpenSlide generic TIFF](https://openslide.org/formats/generic-tiff/).
